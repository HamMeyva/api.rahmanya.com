# --- 📌 Start::Response Models ---
type AdClick {
    id: ID!
    ad_id: ID!
    user_id: ID!
    click_at: DateTime!
    created_at: DateTime!
    updated_at: DateTime!
}

type CreateAdClickResponse {
    success: Boolean!
    message: String
}
# --- 📌 End::Response Models ---

# --- 📌 Start::Input Models ---
input CreateAdClickInput {
    ad_id: ID!
}
# --- 📌 End::Input Models ---

# --- 📌 Start::Query ---
# --
# --- 📌 End::Query ---

# --- 📌 Start::Mutations ---
extend type Mutation {
    # Reklam tıklaması ekle
    createAdClick(input: CreateAdClickInput!): CreateAdClickResponse!
        @guard(with: ["sanctum"])
        @field(resolver: "App\\GraphQL\\Resolvers\\AdClickResolver@store")
}
# --- 📌 End::Mutations ---