# --- 📌 Start::Response Models ---
type AgoraChannelMessage {
    id: ID
    agora_channel_id: ID
    user_id: String
    user_data: JSON
    admin_id: String
    admin_data: JSON
    message: String
    original_message: String
    has_banned_word: Boolean
    is_pinned: Boolean
    is_blocked: Boolean
    timestamp: Int
    parent_message_id: ID
    gift_id: Int
    gift_data: JSON
}

type SendMessageToStreamResponse {
    success: Boolean!
    message: String,
}

type GetStreamMessagesResponse {
    success: Boolean!
    message: String,
    data: [AgoraChannelMessage!]
}
# --- 📌 End::Response Models ---

# --- 📌 Start::Input Models ---
input SendMessageToStreamInput {
    agora_channel_id: ID!
    message: String!
}

input GetStreamMessagesInput {
    agora_channel_id: ID!
    page: Int
    limit: Int
    dir: String
}
# --- 📌 End::Input Models ---

# --- 📌 Start::Query ---
extend type Query {
    getStreamMessages(input: GetStreamMessagesInput!): GetStreamMessagesResponse!
        @guard(with: ["sanctum"])
        @field(resolver: "App\\GraphQL\\Resolvers\\Agora\\AgoraMessageResolver@getStreamMessages")
}
# --- 📌 End::Query ---

# --- 📌 Start::Mutations ---
extend type Mutation {
    sendMessageToStream(input: SendMessageToStreamInput!): SendMessageToStreamResponse!
        @guard(with: ["sanctum"])
        @field(resolver: "App\\GraphQL\\Resolvers\\Agora\\AgoraMessageResolver@sendMessageToStream")
}
# --- 📌 End::Mutations ---
